export type SessionVaultRecord = { id:string; tenantId:string; userId:string; computerId:string; browserProfileId:string; platform:string; encryptedCookieJarRef:string; encryptedLocalStorageRef?:string; encryptedDeviceBindingRef?:string; storageRegion:string; keyVersion:string; status:"valid"|"expired"|"requires_user_verification"|"revoked"; createdAt:string; lastUsedAt?:string; expiresAt?:string; };
